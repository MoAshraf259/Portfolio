# Base path injected into the Vite build. Use "/" locally and "/Portfolio/" for GitHub Pages.
VITE_BASE_PATH=/

# Leave unset unless you restore the backend API.
# VITE_API_BASE_URL=https://example.com/api/v1
